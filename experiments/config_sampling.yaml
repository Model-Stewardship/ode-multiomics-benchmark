experiment_name: reduced_sampling
n_patients: 500
noise_sigma: 0.10
n_timepoints: 2
ode_n_vars: 6
n_replicates: 3
random_seed: 42

ude:
  n_epochs: 500
  lr: 0.001
  hidden_dim: 32
  batch_size: 32

sindy:
  degree: 2
  threshold: 0.05

output_dir: results/
