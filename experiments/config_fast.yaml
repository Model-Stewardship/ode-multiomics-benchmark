experiment_name: fast_dev
n_patients: 50
noise_sigma: 0.10
n_timepoints: 6
ode_n_vars: 6
n_replicates: 2
random_seed: 42

motif:
  n_restarts: 1

ude:
  n_epochs: 50
  lr: 0.001
  hidden_dim: 32
  batch_size: 16

sindy:
  degree: 2
  threshold: 0.05

output_dir: results/
